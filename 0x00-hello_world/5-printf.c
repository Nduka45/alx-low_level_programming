#include <stdio.h>

/**
 * main - Entry point "proper grammar"
 *               
 *
 * Return: Always 0 (pass)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
